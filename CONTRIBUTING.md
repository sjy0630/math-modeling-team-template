# 团队协作规则

## 基本流程

1. 开始前先同步最新 `main`。
2. 一个交付开一个短分支，例如 `analysis/q1-exploration`、`model/q2-baseline`、`paper/abstract-v1`。
3. 每次提交只做一件能说清的事，不提交缓存、临时导出或原始大数据。
4. 通过 Pull Request 说明输入、输出、关键假设、验证方式和仍存在的风险。
5. 至少一名队友复核后再合并；合并后其他人立即同步 `main`。

## 文件责任

- `notebooks/` 允许探索，但最终结论必须落到 `src/`、`outputs/` 或 `paper/`。
- `src/` 中的脚本应能从项目根目录运行，并把路径写成相对路径。
- `outputs/` 只保留会被论文引用或用于验收的结果。
- 改变需求、利润、损耗或退货口径时，必须同步 `decision_log.md`。
- 使用 AI 生成或修改关键分析时，必须同步 `ai_usage_log.md`。

## PR 最低验收

- [ ] 能说明输入文件、输出文件与单位。
- [ ] 不含绝对本机路径、账号、密钥或隐私信息。
- [ ] 没有使用未来数据预测过去。
- [ ] 关键图表有标题、坐标、单位和一句结论。
- [ ] 关键数字能由脚本重新生成。
- [ ] 至少一名非作者能复述方法及限制。
